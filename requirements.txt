# Core
pandas>=2.0.0
pyarrow>=12.0.0

# Retrieval funnel (Phase 4)
rank-bm25>=0.2.2
sentence-transformers>=2.7.0

# LLM judge (Phase 5)
google-generativeai>=0.7.0
aiohttp>=3.9.0

# Ranking (Phase 6-8)
# (no additional deps — rank.py is CPU-only, no network)
